# Vercel AI Gateway — pulled automatically via `vercel env pull .env.local`
# (VERCEL_OIDC_TOKEN is injected; no separate API key needed on Vercel)
VERCEL_OIDC_TOKEN=

# Override the default gateway model (openai/gpt-4o-mini if unset).
VERCEL_AI_GATEWAY_MODEL=
