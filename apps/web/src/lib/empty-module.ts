// Empty module stub. Resolves imports of Node.js built-ins that are only
// required server-side (see apps/web/next.config.ts) when the client bundle
// traces them transitively through agent modules. Never import this directly.
export {};
